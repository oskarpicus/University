package validator;

import domain.Participant;

public class ParticipantValidator implements Validator<Long, Participant> { //todo
    @Override
    public void validate(Participant entity) {

    }
}
